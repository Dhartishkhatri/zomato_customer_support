# Copy to .env (or just export these) before running the demo.
# The SDK also accepts an `ant auth login` profile instead of a key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: turn the Claude vision judge off and run forensics locally only.
# ZOMATO_AGENT_OFFLINE_FORENSICS=1
